# Decisions for BW-M3: remaining Suggesters

| ID | Date | Decision | Source | Rationale | Impact |
| --- | --- | --- | --- | --- | --- |
| d001 | 2026-05-20 | BW-M3 treats the B depsolve duplicate BuildRequires case as a required guard, not a workflow change. | BW-M2 review; `.dev_memory/bw_m2_workflow/known_issues.md`. | The workflow should still route depsolve by `primary_error.kind`, but the Suggester must avoid creating a useless duplicate patch. | `DepsolveSuggester` will emit advisory guidance when the dependency is already declared. |
| d002 | 2026-05-20 | Advisory Suggesters emit markdown-only suggestions and never source patches. | `docs/build_workflow/DESIGN.md` §4.4-§4.7. | Linker undef, patch failed, spec script, compile error, and fallback cases require user intent and should not pretend to fix source automatically. | These Suggesters set `patch_content=None`, `confidence=advisory`, and detailed manual steps. |
| d003 | 2026-05-20 | `LinkerMissingSuggester` may emit a low-confidence `-devel` BuildRequires candidate patch. | `docs/build_workflow/DESIGN.md` §4.5. | Missing `-lfoo` can sometimes be addressed by installing a development package, but v0.1 has no repository knowledge. | The patch is reversible and `git apply` compatible, but markdown risks warn that provider names or `-L` paths may be the real fix. |
| d004 | 2026-05-20 | `FallbackSuggester` matches only unsupported primary kinds and remains last in the registry. | `docs/build_workflow/DESIGN.md` §4.5; workflow summary risk table. | Fallback should not add generic noise when a known Suggester already handles the primary kind. | Unknown kinds produce one advisory fallback; known A/B/C/D kinds route to their specific Suggester. |
| d005 | 2026-05-20 | BW-M3 validates Suggester routing with unit tests; real A/B/C/D E2E stays in BW-M4. | `docs/build_workflow/DESIGN.md` §6-§7. | The current milestone is Suggester implementation, while BW-M4 explicitly owns fixture E2E and Cline integration. | BW-M3 test report records focused Suggester coverage and full regression gates, not real gbs runs. |
