# Known Issues for M0

- The local environment does not have `gh` installed.
- Local HTTPS `git push` currently lacks credentials.
- The GitHub connector can read `lhmax2010/LogAnalysisSkill` but reports no push permission.

These are publishing/authentication blockers, not repository-structure blockers.
