# M0 Perf Baselines

M0 has no analyzer runtime, so there is no `perf_report.json` baseline yet.

The first runtime performance baseline is expected during M1 after Layer 0+1 scanning
and tracing foundation exist.
