# Decisions for M8: wrapper_and_e2e

| ID | Date | Decision | Source | Rationale | Impact |
| --- | --- | --- | --- | --- | --- |
| d001 | 2026-05-18 | Start M8 from merge commit `18e4d33` after M7 review approval and merge. | v0.5 §7, §8, §13 | Keeps the MVP acceptance branch aligned with reviewed `main`. | M8 branch starts from the latest merged baseline and owns wrapper/e2e delivery. |
| d002 | 2026-05-18 | The M8 wrapper grants Level 2 (600 tokens) by default, but preserves Level 3 for `undefined_reference` because its tier2 pattern requires `symbol_context`. | v0.5 §3.4, §5, §10.3; M5 d005 | Most MVP tier2 patterns require Level 2 evidence, while linker undefined-reference tier2 needs Level 3 symbol context. This keeps common packets within BudgetPool while preserving one link tier2 acceptance path. | E2E tier2 direct answers stay non-degraded for compile/spec/deps and undefined-reference link cases when fixture evidence is compact enough for the 1400-token BudgetPool. |
| d003 | 2026-05-18 | Use 5 Fast-Path E2E fixtures instead of only the 4 inherited M2 fixtures. | v0.5 §10.3, §13; M8 startup instruction | MVP acceptance requires Fast-Path hit rate ≥25%. With 20 fixtures, 4 hits would be 20%, so at least 5 real Fast-Path fixtures are mathematically required. | E2E includes the 4 M2 categories plus one additional patch variant; the test asserts the aggregate hit rate instead of tuning expected output after the fact. |
