# Decisions for restructure_phase2_analyzer

| ID | Date | Decision | Source | Rationale | Impact |
| --- | --- | --- | --- | --- | --- |
| d001 | 2026-05-25 | Move `gbs_analyzer` under `tizen-gbs-log-analysis/scripts/` while preserving the import package name. | User-approved repository-as-published-skill structure, Phase 2 scope. | The skill hub should show a clean kebab-case analyzer skill folder, but existing tests and workflow code import `gbs_analyzer.*`. Preserving the package name avoids analysis logic churn. | Analyzer code and pattern data move as a unit; imports remain `gbs_analyzer.*`. |
| d002 | 2026-05-25 | Keep `SKILL.md` at `tizen-gbs-log-analysis/SKILL.md`, not inside `scripts/`. | User clarification for symmetric skill publish folders. | The publish skill root should contain `SKILL.md` and `scripts/`, matching Phase 1. | Analyzer skill metadata is moved to the skill root; package code stays under `scripts/gbs_analyzer/`. |
| d003 | 2026-05-25 | Add `scripts/run_analyzer.py` as a dual-mode launcher. | Phase 2 requirement for installed and direct skill-folder modes. | Installed mode should use normal package imports. Direct folder mode should add only its own `scripts/` directory to `sys.path` when `gbs_analyzer` is not installed. | Users can run either `python -m gbs_analyzer` after install or `python tizen-gbs-log-analysis/scripts/run_analyzer.py` from a local skill checkout. |
| d004 | 2026-05-25 | Move `patterns/` with the analyzer package and rely on existing `Path(__file__)` anchors. | hotfix_real_smoke_003 package-data fix and user Phase 2 warning. | `quick_filter.py` and `semantic_classifier.py` already resolve pattern files relative to the package; the internal relative layout is preserved after the move. | Pattern data works in both site-packages and direct skill-folder modes without changing pattern contents or analyzer logic. |
