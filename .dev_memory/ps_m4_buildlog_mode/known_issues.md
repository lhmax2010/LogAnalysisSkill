# Known Issues for PS-M4 Buildlog Mode

- Workflow integration remains PS-M6.
- Real direct-folder Cline validation should confirm sibling analyzer discovery outside the repo root.
- Analyzer `src-root auto -> buildlog parent` can still be wrong for real GBS paths; this is tolerated by Level B fallback.

