# Decisions for restructure_phase3_workflow

| ID | Date | Decision | Source | Rationale | Impact |
| --- | --- | --- | --- | --- | --- |
| d001 | 2026-05-26 | Move `gbs_workflow` under `tizen-gbs-build-workflow/scripts/` while preserving the import package name. | User-approved repository-as-published-skill structure, Phase 3 scope. | The workflow should be visible as its own publishable skill while tests and installed-mode users keep importing `gbs_workflow.*`. | Workflow code moves as a package-preserving rename; business logic remains unchanged. |
| d002 | 2026-05-26 | Add `scripts/run_workflow.py` with installed-mode import first and direct sibling discovery second. | User Phase 3 launcher requirements. | Installed mode should use normal package imports. Direct skill-folder mode should discover sibling `tizen-gbs-build` and `tizen-gbs-log-analysis` without vendoring their code. | Users can run either `python -m gbs_workflow` after install or `python tizen-gbs-build-workflow/scripts/run_workflow.py` from sibling skill folders. |
| d003 | 2026-05-26 | Pass sibling scripts paths to the analyzer subprocess through an env copy with `PYTHONPATH` prepended. | Phase 3 subprocess PYTHONPATH warning. | `sys.path` changes only affect the workflow process; `python -m gbs_analyzer` runs in a child process and needs explicit `PYTHONPATH`. The env copy preserves any user `PYTHONPATH` without mutating `os.environ`. | Direct sibling mode can run the analyzer subprocess; installed mode remains unaffected. |
| d004 | 2026-05-26 | Support env-var dependency discovery before sibling layout. | User requirement for `TIZEN_GBS_BUILD_SKILL_DIR` and `TIZEN_GBS_LOG_ANALYSIS_SKILL_DIR`. | Real deployments may not place the three skill folders next to each other. Env vars provide a deterministic override while keeping sibling layout as the default. | Launcher works with both side-by-side folders and explicitly configured dependency locations. |
| d005 | 2026-05-26 | Add `mypy_path` for all three scripts roots. | Phase 3 package-root relocation. | Once packages live under separate skill folders, mypy needs all source roots to resolve cross-skill imports during path-based checks. | Local and CI type checks can validate analyzer and workflow paths without relying on root package directories. |
