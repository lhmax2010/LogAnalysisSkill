# Integration Examples

M8 provides example contracts for external callers:

- `cline/`: custom command configuration for Cline-style invocation.
- `compiling_agent/`: subprocess adapter for unattended build-monitoring agents.

These examples document how to call `python -m gbs_analyzer analyze`. They are not
live integrations and are not deployed by this repository.
