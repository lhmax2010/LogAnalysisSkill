# Patterns

Pattern files are introduced in later milestones.

Expected files from the v0.5 design:

- `gbs_errors.yaml`
- `error_semantics.yaml`
- `_pending.yaml`
- `schema.json`

M0 keeps this directory as a documented placeholder only.
